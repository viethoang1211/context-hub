{
  "name": "@context-hub/cli",
  "version": "0.1.0",
  "description": "CLI for Context Hub - search and retrieve LLM-optimized docs and skills",
  "type": "module",
  "bin": {
    "chub": "./bin/chub"
  },
  "scripts": {
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "tar": "^7.0.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
